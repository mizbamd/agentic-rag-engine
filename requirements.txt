# Core runtime (the RAG core itself has zero third-party deps).
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6

# Agent orchestration (LangGraph). Optional: SimpleAgent works without it.
langgraph>=0.2
langchain-core>=0.3

# Dev/test
pytest>=8
httpx>=0.27
